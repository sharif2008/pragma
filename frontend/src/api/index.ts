export * from './types';
export * from './client';
export { paths } from '../services/paths';

/** OpenAPI-style request/response types (same symbols as root exports, grouped for `Schemas.*` usage). */
export type * as Schemas from './types';
